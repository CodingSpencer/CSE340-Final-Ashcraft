<h1>Manage Reviews</h1>

<%- include('../../partials/flash-messages') %>

<% if (reviews && reviews.length > 0) { %>
    <div class="review-cards">
        <% reviews.forEach((review) => { %>
            <div class="review-card admin">
                <div class="review-card-header">
                    <span class="review-author">
                        <strong>Customer:</strong> <%= review.user ? review.user.name : 'Unknown' %>
                    </span>
                    <span class="review-vehicle">
                        <strong>Vehicle:</strong> 
                        <% if (review.vehicle) { %>
                            <a href="/inventory/<%= review.vehicle_id %>"><%= review.vehicle.year %> <%= review.vehicle.make %> <%= review.vehicle.model %></a>
                        <% } else { %>
                            Unknown Vehicle
                        <% } %>
                    </span>
                    <span class="review-rating">
                        <% for (let i = 1; i <= 5; i++) { %>
                            <span class="star <%= i <= review.rating ? 'filled' : '' %>">&#9733;</span>
                        <% } %>
                    </span>
                </div>
                <div class="review-card-body">
                    <p><%= review.review_text %></p>
                </div>
                <div class="review-card-footer">
                    <span class="review-date">Posted: <%= new Date(review.created_at).toLocaleDateString() %></span>
                    <div class="review-actions">
                        <form method="POST" action="/admin/reviews/<%= review.id %>/delete" style="display:inline;">
                            <button type="submit" class="btn btn-danger btn-sm" data-confirm="Are you sure you want to delete this review?">Delete Review</button>
                        </form>
                    </div>
                </div>
            </div>
        <% }) %>
    </div>
<% } else { %>
    <p>No reviews have been submitted yet.</p>
<% } %>

<div class="dashboard-link">
    <a href="/dashboard">&larr; Back to Dashboard</a>
</div>